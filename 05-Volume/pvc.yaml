apiVersion: v1
kind : PersistentVolumeClaim
metadata:
  name: pvc-han
spec:
  storageClassName: han-disk
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi